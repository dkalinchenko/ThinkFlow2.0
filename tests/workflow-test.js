const puppeteer = require("puppeteer"); async function runTest() { console.log("Starting workflow test"); const browser = await puppeteer.launch({headless: false, slowMo: 100}); const page = await browser.newPage(); try { await page.goto("http://localhost:3333"); console.log("Step 1: Homepage loaded"); await page.screenshot({path: "screenshots/01-homepage.png"}); console.log("Step 2: Finding New Decision button"); const links = await page.21928eval("a", links => links.map(a => ({href: a.href, text: a.innerText}))); console.log("Found links:", JSON.stringify(links)); console.log("Test completed"); } catch (error) { console.error("Test error:", error); } finally { await browser.close(); } } runTest();