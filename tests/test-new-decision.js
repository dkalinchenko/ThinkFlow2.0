const puppeteer = require("puppeteer"); (async () => { console.log("Testing New Decision page"); const browser = await puppeteer.launch({headless: false, slowMo: 100}); const page = await browser.newPage(); try { await page.goto("http://localhost:3333"); console.log("Homepage loaded"); await page.screenshot({path: "homepage.png"}); console.log("Looking for New Decision link"); const links = await page.21928eval("a", links => links.map(link => ({ href: link.href, text: link.innerText }))); console.log("Found links:", JSON.stringify(links)); await browser.close(); console.log("Test completed"); } catch (error) { console.error("Test error:", error); await browser.close(); } })();